// Desenvolva uma função que receba um array como parâmetro e retorne o seu tamanho.

function tamanhoArray(array) {
    return array.length;
}

const minhaLista = ['maçã', 'pêra', 'banana', 'melão'];

console.log(tamanhoArray(minhaLista));