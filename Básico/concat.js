/* 1) Adicione os números 1,2,3 em uma variável chamada minhaLista e armazene os números 4, 5 e 6 separadamente em outra variável. Use o método concat() para combinar as sequências de valores em uma nova lista chamadanovaLista. Imprima novaLista no console. 

function metodoConcat() {
    let minhaLista = [1, 2, 3];
    let outraLista = [4, 5, 6];
    let novaLista = minhaLista.concat(outraLista);
    console.log(novaLista); 
    }
*/

/* 2) Remova o último elemento de novaLista. Imprima novaLista após a remoção
function metodoConcat() {
    let minhaLista = [1, 2, 3];
    let outraLista = [4, 5, 6];
    let novaLista = minhaLista.concat(outraLista);
    console.log(novaLista);
    let removendoUltimoElemento = novaLista.pop();
    console.log(novaLista); */

